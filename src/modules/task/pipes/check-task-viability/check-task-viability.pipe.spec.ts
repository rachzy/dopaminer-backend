import { CheckTaskViabilityPipe } from './check-task-viability.pipe';

describe('CheckTaskViabilityPipe', () => {
  it('should be defined', () => {
    expect(new CheckTaskViabilityPipe()).toBeDefined();
  });
});
