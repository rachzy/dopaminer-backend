import { ValidUserInterceptor } from './valid-user.interceptor';

describe('ValidUserInterceptor', () => {
  it('should be defined', () => {
    expect(new ValidUserInterceptor()).toBeDefined();
  });
});
